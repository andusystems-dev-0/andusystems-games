{
  "name": "andusystems-mobile-shell",
  "private": true,
  "version": "0.0.0",
  "description": "Generic offline Capacitor shell that renders any game's web bundle (no per-game native code).",
  "scripts": {
    "prepare-app": "node scripts/prepare.mjs"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/preferences": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0",
    "@capacitor/android": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  }
}
